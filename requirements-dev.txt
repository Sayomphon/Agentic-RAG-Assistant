# Install the exact runtime dependencies used by the application.
-r requirements.txt

# Tests intentionally use Python's standard-library ``unittest`` runner.
# Track A runtime dependencies, including Thai tokenization and local
# reranking, are pinned in requirements.txt so every environment is identical.
